<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:LogisticForStudent"
             Title="Noticias"
             x:Class="LogisticForStudent.Views.Noticiasxaml">
    <ContentPage.BindingContext>
        <local:Guiasinf />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding Itemslist}"></ListView>
            <ListView x:Name="lstguias"></ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>